/**
 * Description: Get and show CPUs numbers and props.
 */

/** Require generics dependences */
import os from 'os';
import 'pretty-console-colors';

// Show all cpus and props.
console.log(os.cpus());
