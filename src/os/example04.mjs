/**
 * Description: Get and show Host Name Machine.
 */

/** Require generics dependences */
import os from 'os';
import 'pretty-console-colors';

// Show host name machine.
console.log(os.hostname());
