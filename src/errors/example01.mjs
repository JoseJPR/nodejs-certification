/**
 * Description: Variable no defined into try/catch.
 */

/** Import generics dependences */
import 'pretty-console-colors';

const apples = 1;

try {
  const result = apples + other;
  console.log(result);
} catch (e) {
  console.error(e);
}